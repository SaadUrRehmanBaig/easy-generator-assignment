name: Example Workflow
on: [push]

jobs:
  example-job:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Print all env vars
        run: |
          KEY="ABC"
          echo "${!KEY}"
          echo "$VARS"
        env:
          VARS: ${{ toJSON(vars) }}

