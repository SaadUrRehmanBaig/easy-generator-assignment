name: Example Workflow
on: [push]

jobs:
  example-job:
    runs-on: ubuntu-latest
    environment: dev
    env:
      VARS: ${{ job.environment }}
    steps:
      - name: Print all env vars
        run: echo $VARS

